#!/usr/bin/env bash
# Discord DM 알림 전송 — Discord Bot API 직접 호출 (LLM 우회)
# 사용법: hermes-notify "메시지"

set -euo pipefail

MSG="${1:-}"
if [ -z "$MSG" ]; then
    echo "Usage: hermes-notify <message>" >&2
    exit 1
fi

python3 /home/ksh/infra/scripts/discord-dm.py "[Claude Code 보고] $MSG"
