image:
  repository: ghcr.io/ggml-org/llama.cpp
  tag: server-cuda

port: 8000

# Q4_K_M: 5.3GB — RTX 3080 10GB에 적합
model:
  repo: "kai-os/Carnice-9b-GGUF"
  file: "Carnice-9b-Q4_K_M.gguf"

resources:
  limits:
    nvidia.com/gpu: "1"
  requests:
    nvidia.com/gpu: "1"

persistence:
  enabled: true
  size: 50Gi
  storageClass: local-path
