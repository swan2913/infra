apiVersion: apps/v1
kind: Deployment
metadata:
  name: hermes
  namespace: {{ .Release.Namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hermes
  template:
    metadata:
      labels:
        app: hermes
    spec:
      containers:
        - name: hermes
          image: {{ .Values.image.repository }}:{{ .Values.image.tag }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["hermes", "gateway", "run"]
          env:
            - name: HERMES_HOME
              value: /opt/data
            - name: DISCORD_BOT_TOKEN
              valueFrom:
                secretKeyRef:
                  name: hermes-discord
                  key: bot-token
            - name: DISCORD_ALLOWED_USERS
              valueFrom:
                secretKeyRef:
                  name: hermes-discord
                  key: allowed-users
                  optional: true
            - name: DISCORD_IGNORE_NO_MENTION
              value: {{ .Values.discord.ignoreMention | quote }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            - name: data
              mountPath: /opt/data
            - name: config
              mountPath: /opt/data/config.yaml
              subPath: config.yaml
      volumes:
        - name: data
          {{- if .Values.persistence.enabled }}
          persistentVolumeClaim:
            claimName: hermes-data
          {{- else }}
          emptyDir: {}
          {{- end }}
        - name: config
          configMap:
            name: hermes-config
